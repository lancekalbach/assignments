import React from "react"

export default function ColorBox(props) {

    //const [color, setColor] = useState('#000000')



    return (
        <div>

        </div>
    )
}

// className="color-box" style={{background: `${color}`}} put in div if needed